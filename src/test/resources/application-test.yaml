spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/transakt_test
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  data:
    redis:
      database: 1
  flyway:
    baseline-on-migrate: false

ratelimit:
  requests-per-minute: 10000

  bank:
    decline-rate: 0
    latency:
      min-ms: 0
      max-ms: 0