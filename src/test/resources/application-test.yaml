spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/transakt_test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  data:
    redis:
      database: 1

ratelimit:
  requests-per-minute: 10000